import styled from "styled-components";

export const MainStyle = styled.main`

	section.itens{
		margin-top: 10vw;
		display: flex;
		flex-direction: row;
		flex-wrap: wrap;
		justify-content: center;
		gap: 4vw;
	}
`;